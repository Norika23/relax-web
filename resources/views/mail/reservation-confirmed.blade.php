<h1>ご予約ありがとうございます</h1><p>{{ $reservation->customer_name }}さま</p><p>以下の内容でご予約を承りました。</p><ul><li>日時：{{ $reservation->starts_at->setTimezone($reservation->shop->timezone)->format('Y年n月j日 H:i') }}</li><li>メニュー：{{ $reservation->service->name }}</li><li>スタッフ：{{ $reservation->staff->name }}</li><li>合計：{{ number_format($reservation->total_price) }}円</li></ul><p>{{ $reservation->shop->name }}</p>
