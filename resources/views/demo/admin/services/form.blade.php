<form class="admin-form" method="post" action="{{ $service?route('demo.admin.services.update',$service):route('demo.admin.services.store') }}">@csrf @if($service)@method('put')@endif<div class="field-grid"><label>メニュー名<input class="booking-input" name="name" value="{{ old('name',$service?->name) }}" required></label><label>料金<input class="booking-input" type="number" name="price" value="{{ old('price',$service?->price??0) }}" required></label><label>施術時間（分）<input class="booking-input" type="number" name="duration_minutes" value="{{ old('duration_minutes',$service?->duration_minutes??60) }}" required></label><label>準備時間（分）<input class="booking-input" type="number" name="buffer_minutes" value="{{ old('buffer_minutes',$service?->buffer_minutes??15) }}" required></label><label>表示順<input class="booking-input" type="number" name="display_order" value="{{ old('display_order',$service?->display_order??0) }}"></label><label>説明<textarea class="booking-input" name="description">{{ old('description',$service?->description) }}</textarea></label></div><label><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$service?->is_active??true))> 公開する</label><button class="booking-primary">保存</button></form>
